apiVersion: v1
kind: Service
metadata:
  name: resource-db
  namespace: {{ .Values.namespace }}
spec:
  clusterIP: None
  selector:
    app: resource-db
  ports:
    - port: 5432
      targetPort: 5432
