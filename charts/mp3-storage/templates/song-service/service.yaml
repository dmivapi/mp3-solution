apiVersion: v1
kind: Service
metadata:
  name: song-service
  namespace: {{ .Values.namespace }}
spec:
  type: NodePort
  selector:
    app: song-service
  ports:
    - port: 8091
      targetPort: 8091
      nodePort: {{ .Values.songServicePort }}
