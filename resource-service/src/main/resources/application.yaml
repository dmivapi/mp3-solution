server:
  port: 8080
song-service:
  client:
    base-url: "http://localhost:8090"
spring:
  application:
    name: resource-service
  datasource:
    url: jdbc:postgresql://localhost:5432/resource-db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
