server:
  port: 8090
spring:
  application:
    name: song-service
  datasource:
    url: jdbc:postgresql://localhost:5433/song-db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
